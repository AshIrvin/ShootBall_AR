using Unity.Netcode;
using UnityEngine;
using UnityEngine.UIElements;

public class NetworkButtons : MonoBehaviour
{
    /*
    private GUIStyle gUIStyle = new();
    [SerializeField] private int fontSize = 40;
    private void OnGUI()
    {
        GUILayout.BeginArea(new Rect(50, Screen.height/2, 200, 750));
        
        if (!NetworkManager.Singleton.IsClient && !NetworkManager.Singleton.IsServer)
        {
            
            gUIStyle.fontSize = fontSize;

            if (GUILayout.Button("Host", gUIStyle)) NetworkManager.Singleton.StartHost();
            if (GUILayout.Button("Server", gUIStyle)) NetworkManager.Singleton.StartServer();
            if (GUILayout.Button("Client")) NetworkManager.Singleton.StartClient();
        }

        GUILayout.EndArea();
    }
    */
}


